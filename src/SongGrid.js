const SongGrid = (songs) => {
  console.log({ songs });
  return <div className="relative block bg-white p-3 pb-6"></div>;
};

export default SongGrid;

{
  /* 
              songs.items.map((song) => {
                  return <SongGrid key={song.track.id} song={song} />;
                }) */
}
